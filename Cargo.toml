[package]
name = "lau-render"
version = "0.1.0"
edition = "2021"
description = "Deterministic pure-CPU software rasterizer"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
